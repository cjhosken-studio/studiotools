import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/public/pipeline/studiotools/',
  publicDir: false, // Prevents infinite recursion copy loop when outDir is inside public
  build: {
    outDir: 'public/pipeline/studiotools',
    emptyOutDir: true,
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      }
    }
  }
})
